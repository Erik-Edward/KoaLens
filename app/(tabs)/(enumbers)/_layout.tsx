import { Stack } from 'expo-router';
import { Ionicons } from '@expo/vector-icons';

export default function EnumbersLayout() {
  return (
    <Stack
      screenOptions={{
        headerShown: false,
      }}
    />
  );
} 